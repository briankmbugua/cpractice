#include<stdio.h>
/*
we can also provide another name or name alias to the pointer variables
using typedef
*/

int main(){
    int *ptr;
    typedef int* ptr;
    /*in the above statement, we have declared the variable of type int*.
    we can create the variable of type int* by simply using the 'ptr'
    variable as shown below*/

    ptr p1;
}